# ============================================================
# MecaIA V3 — Configuration Netlify
# ============================================================
[build]
  command = "echo 'No build step'"
  functions = "netlify/functions"
  publish = "."

# Ces clés sont PUBLIQUES (identifiants de prix, clé publique Stripe, URL + clé anon
# Supabase) : normal qu'elles apparaissent côté client. On dit au scanner de Netlify
# de ne PAS les traiter comme des secrets. Les vrais secrets (STRIPE_SECRET_KEY,
# SUPABASE_SECRET, ANTHROPIC_KEY, RESEND_API_KEY, STRIPE_WEBHOOK_SECRET) restent scannés.
[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "OWNER_EMAIL,SUPABASE_URL,SUPABASE_ANON,STRIPE_PUBLIC_KEY,FRONTEND_URL,STRIPE_PRICE_1CREDIT,STRIPE_PRICE_25CREDITS,STRIPE_PRICE_60CREDITS,STRIPE_PRICE_UNLIMITED,STRIPE_PRICE_1EUR,STRIPE_PRICE_5EUR,STRIPE_PRICE_10EUR,STRIPE_PRICE_15EUR"

[functions]
  # esbuild embarque les imports (../lib/auth.mjs) et les node_modules
  node_bundler = "esbuild"
  # Timeout étendu pour les fonctions longues (dylan_agents conclusion Sonnet)
  # Netlify Pro : 26s max — Netlify Free : 10s max (capé automatiquement)
  timeout = 26

# Fichiers statiques spéciaux à NE PAS intercepter par la SPA
# (robots.txt, sitemap.xml, manifest.json doivent être servis tels quels)
[[redirects]]
  from = "/robots.txt"
  to = "/robots.txt"
  status = 200
  force = false

[[redirects]]
  from = "/sitemap.xml"
  to = "/sitemap.xml"
  status = 200
  force = false

# Les routes /.netlify/functions/* sont gérées nativement par Netlify.
# SPA : tout le reste renvoie index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# ============================================================
# SECURITY HEADERS
# ============================================================
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"
    Strict-Transport-Security = "max-age=31536000; includeSubDomains; preload"

# ------------------------------------------------------------
# Pipeline planifié (accumulation de cas) — lundi & jeudi 3h UTC
# ------------------------------------------------------------
# ⏸️ DÉSACTIVÉ POUR LA BÊTA (2026-06) — évite des appels IA inutiles et
# protège la base de connaissance. Les 5 fonctions du pipeline restent en
# place (scraper / cleaner / consolidator / orchestrator / scheduled_pipeline).
# Pour RÉACTIVER après lancement : retirer les "#" ci-dessous + corriger le
# mapping de colonnes dans insert_consolidated_case (vehicle_year / obd_code).
#
# [[scheduled]]
#   cron = "0 3 * * 1"
#   function = "scheduled_pipeline"
#
# [[scheduled]]
#   cron = "0 3 * * 4"
#   function = "scheduled_pipeline"